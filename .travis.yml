language: c
os:
  - linux
  - osx
notifications:
  email: false
script:
  - sudo apt-get install gfortran
  - make
deploy:
  provider: releases
  api_key: "$OATH_TOKEN"
  file: "src/fortran-code.*"
  file_glob: true
  skip_cleanup: true
  on:
    tags: true
